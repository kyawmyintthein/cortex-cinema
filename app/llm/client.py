class OpenAIClient:
    """Placeholder boundary for OpenAI API calls."""

    def responses_create(self, prompt_name: str, payload: dict) -> dict:
        return {
            "prompt": prompt_name,
            "payload": payload,
        }
