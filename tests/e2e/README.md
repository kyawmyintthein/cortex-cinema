# Local E2E

Use `scripts/run_local_e2e.sh` to build the Docker image, start the app with Docker Compose, and run the smoke test container.

The main entrypoint is also exposed through `just test-local`.
